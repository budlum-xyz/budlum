[package]
name = "agent-knowledge"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The closed-circuit knowledge layer: secret masking, chunking, TF-IDF embedding, compact context, task memory and the LLM output cache"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[lints]
workspace = true
