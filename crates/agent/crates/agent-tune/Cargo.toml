[package]
name = "agent-tune"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The training plan (LoRA BF16/FP16 - FP4 does not exist in the type system) and the output hash lock"

[dependencies]
agent-core = { path = "../agent-core" }
agent-data = { path = "../agent-data" }

[lints]
workspace = true
