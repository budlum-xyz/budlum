[package]
name = "ai-data"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The closed-circuit data layer: source review, JSONL records and SHA-256 verification"

[dependencies]
ai-core = { path = "../ai-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[lints]
workspace = true
